var nome = 'MaRcElLo';

console.log(nome.toLowerCase()); //minusculo
console.log(nome.length); //Qtd de caracteres

var altura = 1.1;

console.log(altura.toString()); //transforme em string
console.log(altura.toFixed()); //arredonda pra baixo ou pra cima
